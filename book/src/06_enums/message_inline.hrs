enum Message
    Quit
    Move\ x: i32, y: i32
    Write String
    ChangeColor i32 i32 i32

fn main$:
    let m = Message.Move\ x = 1, y = 2
    if let Message.Move\ x, y = m:
        println! "moved to {x},{y}"
