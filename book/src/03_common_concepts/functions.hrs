fn greet name: &str:
    println! "hello, {}" name

fn add (a: i32) (b: i32) -> i32:
    a + b

fn square x: i32 -> i32:
    let y = x * x
    y

fn main$:
    greet "world"
    println! "{}" (add 2 3)
    println! "{}" (square 9)
