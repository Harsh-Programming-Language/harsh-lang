// Three ways to write the same block.
fn indented (t: bool) -> i32:
    if t:
        1
    else:
        2

fn inline (t: bool) -> i32:
    if t: 1 else: 2

fn braced (t: bool) -> i32:
    if t { 1 } else { 2 }

fn main$:
    println! "{} {} {}" (indented true) (inline false) (braced true)
