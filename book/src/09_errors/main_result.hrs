use std.error.Error
use std.fs.File

fn main$ -> Result<(), Box<dyn Error>>:
    let greeting_file = File.open "hello.txt"
    println! "opened: {}" (greeting_file <- is_ok$)

    let n: i32 = "42" <- parse$?
    println! "{n}"
    Ok ()
