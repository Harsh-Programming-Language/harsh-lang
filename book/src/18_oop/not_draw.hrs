pub trait Draw:
    fn draw (&self)

pub struct Screen
    pub components: Vec<Box<dyn Draw>>

fn main$:
    let screen =
        Screen\
            components = vec! [Box.new (String.from "Hi")]
    let _ = screen
